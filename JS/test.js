var random number = math.random();
Write to DOM
parentEl = document.getElementById(‘id of div element’)
imgEl = document.createElement(‘img’)
var chosenProduct = products[random number]
chosenProduct.filepath = imgEl.src //writes image in img element
var random number 2 = Math.random()
while (random number 2 = random number)
	var random number 2 = math.random();
Write to DOM

var random number 3 = math.random()
while (random number 3 = random number
	var random number 3 = math.random();
write to DOM

function onclickHandler () {
  var randomNumber = Math.floor(Math.random() * 20);
  parentEl = document.getElementById('image-container');
  imgEl = document.createElement('img');
  var chosenProduct = products[randomNumer];
  chosenProduct.filepath = imgEl.src;
  var randomNumberTwo = Math.floor(Math.random() * 20);
  while (randomNumberTwo == randomNumber) {
    var randomNumberTwo = Math.floor(Math.random() * 20;)
  }
  chosenProduct.filepath = imgEl.src;
  var randomNumberThree = Math.floor(Math.random() * 20);
  while (randomNumberThree == randomNumberTwo) {
    var randomNumberThree = Math.floor(Math.random() * 20);
  }
  chosenProduct.filepath = imgEl.src;
}
